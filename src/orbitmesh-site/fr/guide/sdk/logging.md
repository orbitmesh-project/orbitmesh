# Logs

```csharp
PackageHost.WriteInfo("Traité {0} éléments", count);
PackageHost.WriteWarn(...);
PackageHost.WriteError(...);
PackageHost.WriteDebug(...);
```

Apparaît dans la page Console log de la Console, scopé à l'Edge/au package qui l'a écrit.
